# LCK Digital Platform - Project Context

## 1. Суть проекта
Кинематографичный цифровой хаб для Лиги Кинематографистов Казахстана (LCK).
Вайб: "Premium Cinema", Noir, Gold.
Цель: Объединение профи, база знаний, биржа труда, карта локаций.

## 2. Технический Стек (Current State)
*   **Framework:** Next.js 14 (App Router)
*   **Language:** TypeScript
*   **Styling:** Tailwind CSS (Custom config: gold/cinema colors, animations)
*   **Icons:** Lucide React
*   **Deployment:** Vercel
*   **CMS (Content):** Headless WordPress (read-only via API)
*   **DB/Auth (Planned):** Supabase
*   **AI:** Google Gemini API

## 3. Текущая Структура (Key Files)
*   `/app` - Страницы и роутинг (News, Residents, API routes).
*   `/components/ui` - Базовые UI элементы (SpotlightCard, CinematicMist).
*   `/lib` - Утилиты и конфигурации (wpApi.ts, translations.ts).
*   `/types` - Глобальные типы данных.
*   `/context` - Глобальное состояние (темы, язык).

## 4. Текущий статус разработки
*   ✅ Миграция с Vite на Next.js завершена.
*   ✅ Проект собирается и деплоится на Vercel.
*   🔄 **В ПРОЦЕССЕ:** Рефакторинг получения данных (переход на Server Components).
*   ❌ **ОЖИДАЕТ:** Интеграция Supabase Auth.
*   ❌ **ОЖИДАЕТ:** RAG для AI ассистента.

## 5. Правила кодинга (Vibecoding Rules)
1.  **Server-First:** По умолчанию используем Server Components. `use client` добавляем только для интерактивности (хуки, клики, анимации).
2.  **Типизация:** Строгий TypeScript. Никаких `any`.
3.  **Стиль:** Сохранять дизайн-систему (цвета gold/cinema).